# Sover backlog — self-expanding social-media executive (vision roadmap)

- [x] [chore] Privacy (public repo): change `pyproject.toml` `authors` to a project-based attribution — replace `{ name = "Cayleb James" }` with `{ name = "sover contributors" }`. Remove the maintainer's personal name from the published package metadata. Metadata only — no behavior change; the test gate stays green.
- [x] [feature] Beautify the README to top-tier OSS standard — PRESENTATION ONLY, edit ONLY `README.md`, `assets/banner.svg`, and `CONTRIBUTING.md` (do NOT change any `.py`/`.ts` source or behavior). Add: a centered self-contained `assets/banner.svg` (GitHub-safe — no scripts/external refs/fonts), a row of repo-specific shields.io badges (license, top-language, last-commit, repo-size, PRs-welcome, and a CI badge since `.github/workflows` exists), a `mermaid` architecture diagram of the real RESEARCH→COMPOSE→RENDER→MEASURE→MONETIZE capability flow, and a `CONTRIBUTING.md` if absent. Preserve all existing accurate content; do not invent features. Do NOT add any personal name, email, or local path.

- [x] [feature] Launch-gating setup page: the main Sover app must REFUSE to start the engine until the operator has (1) plugged in exactly ONE LLM provider key (Ollama-Cloud OR OpenRouter) and (2) chosen a model — pick from a fetched provider model list OR type one in manually. Add a setup route + page surfaced by the dashboard shell, and a launch-time readiness check (bin/sover_app.py / standalone.py / scripts/providers.py): if no valid key+model, serve the setup page and BLOCK the engine/lanes; once saved (key -> .env, model -> profile.runtime.roles default), boot normally. Validate the key with a cheap provider ping; keys never leave the local .env. Gate: tests that the readiness check blocks when key/model are missing and passes when both are set, and that saving persists both via the existing atomic writers.
- [x] [feature] Build a vision-based pre-flight brand gate (`scripts/vision_judge.py`) that extracts frames from every produced reel and edit-video, scores them against `profiles/<id>/SOUL.md` and the onboarding grid via a vision-capable LLM, and quarantines off-brand/vibe-mismatched content before `content_pipeline._enqueue` — why: it blocks reward-hacking, low-quality posts from reaching followers, preserving brand equity as volume scales.
- [x] [feature] Add a cross-platform autopilot readiness lane (`scripts/platform_readiness.py`) that daily dry-runs `browser_harness.stage_check()` for Instagram and YouTube, records pass/fail streaks, and auto-enables verified platforms in `master.py` publishing once they pass repeatedly — why: it turns the current TikTok-only autopilot into a true 3-platform distribution engine, multiplying views and follower growth.
- [x] [feature] Create a creative A/B variant factory (`scripts/variant_factory.py`) that generates 2-3 hook/caption/thumbnail variants per reel in `content_pipeline.run_batch`, stores them as experiment units in `post_queue.json`, and extends `scorer.py` attribution to `variant_id` so the CGO can bias toward winning hooks — why: it moves optimization from abstract genome knobs to actual creative winners, accelerating the views-to-followers loop.
- [x] [feature] Make `creative_lane.py` attribution-conditioned by feeding `data/scoreboard.json` and `data/attribution.json` into the minimax-m3 prompt, generating quotes and hooks biased toward winning `quote_theme`, `song`, `reel_type`, and `post_hour` before `creative_bank` gates them — why: it makes the creative bank grow directly from what is already driving views instead of from a fixed brand template.
- [x] [feature] Replace static `best_hours` and quiet-hours gating with a contextual publish scheduler (`scripts/scheduler.py`) that picks platform + time per queued post from `data/attribution.json` per-hour performance and content features, respecting `master.py` rate caps — why: it publishes each reel at the slot most likely to earn views rather than a generic timetable.
- [x] [feature] Build an operator memory / directive layer (`scripts/operator_memory.py`) that detects durable preferences and corrections from dashboard chat (`api/routes/chat.py`) and manual posts (`onboarding.observe_recent`), then injects ranked directives into the CGO prompt in `run_pi.py` and overrides `content_pipeline` pickers — why: it converts operator feedback into persistent brand steering so the autonomous loop stays aligned while scaling.
- [x] [architecture] Add a cross-profile meta-learning federation (`scripts/meta_profile.py`) that opt-in aggregates anonymized winning content variables from each profile's `data/attribution.json` into `profiles/_shared/learned_patterns.json`, and seeds new profile onboarding in `onboarding.py` with proven themes/hours/hashtags — why: it lets new accounts bootstrap faster and makes the whole profile portfolio compound toward the north star.
- [ ] [architecture] Build a counterfactual code-mod simulator (`scripts/mutable_evaluator.py`) that, for any proposed `scripts/mutable/` rewrite in `review_queue.py`, replays historical `post_registry.json` + metrics through the new picker and reports predicted score delta before approval — why: it makes the self-improving engine's code mutations evidence-based, reducing risky picker changes.

Ordered, single-increment, gated steps from a fresh chat-only executive outward. Reuse the shipped capability/scaffold/discovery/rebuild machinery; never reinvent.

- [x] HONESTY FIX (prerequisite, no new powers): Rewrite pi/CONTROL.md and pi/SYSTEM.md to describe the ACTUAL tool-less + bounded-action surface — delete the false 'kimi-k2.7-code with read/edit/write/bash tools / edits dashboards / runs jobs' claims and state the real propose-only/whitelisted-action menu. Gate: a test asserting neither doc contains the strings 'read/edit/write/bash' (or equivalent tool claims) and that CONTROL.md names only the verbs the runtime actually executes.
- [x] CHAT READ-CONTEXT (pure read, no actions): Inject a compact STATE block into the chat system prompt — profile.json brand/niche/voice/handles/status, onboarding state, latest followers from account_history, pending proposals, monetization plan from monetization_gate.run(). Edit scripts/api/routes/chat.py only. Gate: a test that the assembled chat prompt contains the live profile id, status, and current follower number from a fixture profile (so the executive answers with real numbers, never hallucinated).
- [x] CHAT ACTION PARSER (reuse run_pi's pattern): Add _execute_chat_actions(reply) to chat.py that extracts a fenced ```json {"actions":[...]} block and dispatches the SAME whitelisted verbs run_pi._execute_cgo_actions already supports — propose -> proposals.propose, need -> needs.flag — and nothing else yet. Each action best-effort + try/except-isolated; never posts/spends/edits. Gate: tests that a reply with a propose action files exactly one proposal and that an unknown action type is ignored without raising.
- [ ] CHAT CAN TRIGGER ONBOARDING (#1 gap, reuse existing endpoint): Add an 'onboard' verb to _execute_chat_actions that calls onboarding.start() (the existing detached _onboard_run flow) behind the existing X-Operator-Confirm gate. First delightful win: 'learn my brand' -> ingest+synthesize -> draft proposal. Gate: a test that the onboard verb invokes onboarding.start for the active profile and is refused without the operator-confirm header.
- [ ] GENESIS API (the missing creation entrypoint): Add POST /sover/profiles/create (X-Operator-Confirm) and a small sover_profile.create(id, brand) that atomically writes profiles/<id>/ (status=draft, minimal profile.json + placeholder SOUL.md) and appends a registry.json row using the EXISTING atomic _write_json + load.cache_clear() pattern. A new profile boots chat-only, everything else locked. Gate: tests that create() registers the profile, sets status=draft, is idempotent on an existing id, and respects single-writer/atomic-write.
- [ ] SCAFFOLD ACTION ON THE BRAIN (first real caller of scaffold()): Add a 'scaffold' action type to run_pi._execute_cgo_actions that calls capabilities.scaffold(cap_id, manifest, files) with status forced to 'proposed' — the brain emits route.py/lane.py/panel.html bodies as STRINGS, never touching bash/file tools. compile()-check each emitted file before write; reject on SyntaxError. Gate: tests that a scaffold action writes capabilities/<id>/ at status=proposed, files a kind=capability proposal, and that a syntactically-broken body is rejected without writing.
- [ ] SCAFFOLD ACTION ON THE CHAT (executive earns its first surface): Add the same 'scaffold' verb to _execute_chat_actions, restricted to cloning vetted templates (start from example_pulse's shape — a panel.html + read-only /cap/<id>/state route, requires_rebuild=false). The executive can now add a SURFACE to itself, operator-approved. Gate: a test that the chat scaffold verb produces a discoverable proposed capability whose code compiles and whose template matches the allowed set.
- [ ] CAPABILITY TEMPLATE LIBRARY + LINT (codegen safety): Add capabilities/_templates/ with vetted RESEARCH/COMPOSE/MEASURE/PANEL templates and a capabilities.lint(files) that runs compile() + forbids imports of master.post / money paths / frozen_guard-protected modules. Wire lint into scaffold() so authoring is template-fill, not arbitrary codegen. Gate: tests that lint passes the templates, rejects a lane importing master.post, and rejects a route shadowing a core path.
- [ ] DECIDE -> REBUILD END-TO-END (prove the loaded gun, no new path): Wire capabilities.decide(approve) (already returns requires_rebuild) so that an operator approval of a requires_rebuild=false capability goes live on next discovery with NO rebuild (dev STATE_ROOT==repo), and only requires_rebuild=true triggers rebuild_relaunch_exe, rate-limited and operator-gated. Gate: a test that a decided requires_rebuild=false capability is picked up by capabilities.routers_for()/lane_specs() and one asserting rebuild is NOT invoked for it.
- [ ] FIRST RESEARCH CAPABILITY niche_radar (read-only, real growth signal): The executive scaffolds capabilities/niche_radar/ — a lane.py that CDP-scrapes the profile's own niche hashtags/competitors (reuse onboarding.py's IG ingest harness) into data/<id>/niche_signals.json, plus a panel.html. Parents: chat + onboarding. Unlocks COMPOSE nodes. Gate: a test that niche_radar's lane.run() writes only under data/<id>/, returns a summary string, and touches no posting/money path.
- [ ] GROWTH PLANNER NODE growth_scout (the supervisor proposes the next move): Add a growth_scout capability/lane that reads supervisor.gather_state() + capabilities.all_caps() + the static capability TREE (parent edges defined per node, NOT brain-chosen) and each cadence emits ONE proposal: the lowest-cost frontier node whose parents are all 'active', ranked by expected follower/engagement leverage, surfaced via capabilities.set_focus(). Gate: tests that growth_scout proposes only parent-satisfied nodes, never fabricates an unlock edge, and emits at most one proposal per cadence.
- [ ] MONETIZATION GRADIENT (money becomes the score, stays human-gated): Re-anchor growth_scout/brain ranking so the leverage score = how much a node shrinks 'days to next LOCKED monetization stream' from monetization_gate.run(). Seed per-profile data/<id>/monetization_stages.json at onboarding accept from a brand-tuned default. No new action types; reuse the gate. Gate: a test that with a near-threshold fixture, growth_scout prioritizes the node that closes the nearest monetization gap.
- [ ] OFFER-ASSEMBLER CAPABILITY (autonomous creative runway, one human click): When the gate moves a stream to READY_APPROVAL, the executive scaffolds capabilities/offer_builder/ whose lane assembles a sign-ready package (affiliate: candidate programs + FTC disclosure + posts to attach; digital_product: brief + price + draft listing) and files needs.flag(kind='money') with the package — passing brand_safety.py. The irreversible enroll/accept/connect-payout stays a single operator record_decision. Gate: tests that offer_builder only fires on READY_APPROVAL, every artifact passes brand_safety, and no code path calls enroll/pay (assert record_decision is a pure ledger write).
- [ ] ATTRIBUTION + RETIREMENT (the tree becomes measurable): Add attribution_radar — a read-only lane joining post_registry to account_history deltas to write per-capability fitness, so each capability node carries a number proving it earned followers/engagement. Feed it to growth_scout so a losing capability is proposed for decide(disabled). Gate: tests that attribution_radar computes per-cap fitness from fixtures and that growth_scout proposes disabling a capability whose fitness is below a floor.
- [ ] CAPABILITY-AUTHORING CAPABILITY (meta frontier, fully self-directed): The executive scaffolds a capability whose subagent.md + lane.py author NEW capability code from the template library when growth_scout detects an unmet, parent-satisfied need with no existing template (e.g. comments full of 'where do you buy this' -> a link-in-bio/storefront capability). 'If it can't be done yet, the agent BUILDS the ability' becomes real — still proposed-only, lint-gated, operator-approved, rebuilt. Gate: tests that the authored capability passes lint, ships only via decide(), and never auto-activates.
