# Lessons

- 2026-06-20T04:45:00Z — Attempted to harden the live-publish drain with persistent browser tabs and post idempotency; shipped on main after 304 tests, but the prior duplicate-post risk came from zero coverage plus committing on scraped URL/stale queue state, so future publish-path changes must start from registry/idempotency tests and commit on the platform published signal rather than URL capture.
- 2026-06-20T05:11:46Z — Attempted cockpit + browser_agent/harness/master/strategy updates → shipped to main, but the diff mixed dashboard UI, frozen/core files, browser behavior, and profile-ignore churn → next time split dashboard changes from RSI-scoped supervisor/agent work and keep generated/profile artifacts out unless explicitly requested.
- 2026-06-20T05:46:32Z — Browser/CDP hardening shipped → the successful path was per-profile browser isolation plus idempotency tests while keeping the dashboard as a static surface → next browser-capability work should add isolated controllers and regression tests before broad cockpit churn.
- 2026-06-20T23:27:37Z — The “engine survives window death + verified bug-fix batch” shipped to main, but frozen-core changes passed because AUTO_BLESS=True auto-rebaselined drift instead of alerting → keep safety-core edits out of broad lifecycle/UI batches and require AUTO_BLESS=False + explicit operator approval before touching frozen files.
- 2026-06-20T23:53:18Z — Hardened the CGO/browser engine against dead CDP windows plus the verified bug-fix batch and retired the old profile launcher → shipped with no gate failure → keep future RSI work focused on supervisor/agent/brand surfaces and avoid reintroducing dashboard self-injection or launcher flows.
- 2026-06-21T00:14:36Z — Attempted a broad Sover RSI batch for engine lifecycle survival, cockpit/profile routing, and workflow bug fixes → shipped after recovering stashed debris and verifying ~315 tests, frozen-core baseline, and the rebuilt exe → next time avoid letting RSI work sit in stash and split large browser/UI/lifecycle changes into smaller commits with smoke tests before consolidation.
- 2026-06-21T01:14:40Z — Chat scaffolding was expanded to recognize direct `scaffold/build <tree node>` and branch-node aliases → shipped after tests passed because aliases are derived from `capability_plan.TREE` and custom-cap fallback remains intact → keep future node-name changes tree-driven and covered by parser+integration tests, not hard-coded lists or dashboard surfaces.
- 2026-06-21T01:41:38Z — Direct tree-node scaffold commands → shipped after parser/tests because chat only matched generic scaffold phrases and missed documented direct commands → future scaffold work should add parser cases for every user-visible tree-node phrase and test the API path, not just planner behavior.
- 2026-06-21T02:45:54Z — Capability-planner beyond-tree expansion was shipped by adding a fixed-tree-completeness gate and pending/rejected-node tests → it shipped because the prior logic could treat an unresolved tree node as “no scaffoldable nodes” and enter exec_growth_* prematurely → next time, keep “tree incomplete / awaiting approval” as a distinct pause state before any beyond-tree proposal and test both pending and rejected nodes.
- 2026-06-21T03:25:42Z — Beyond-tree capability expansion was added behind an explicit fixed-tree-completeness gate → shipped after tests confirmed pending/rejected tree nodes no longer look like “beyond tree” → keep treating a missing, proposed, or rejected fixed-tree node as a pause state, not as permission to create exec_growth_* capabilities.
