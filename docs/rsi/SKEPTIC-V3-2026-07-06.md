All countermeasures audited, wiring traced, tests run, and live deployment state inspected. Final verdict below.

---

## VERDICT: SHIP-WITH-FIXES

The code is unusually honest — I hunted for write-only ledgers and mostly failed to find them; every v3 ledger has a traced reader and a contract test, and the 847/0 test gate reproduces on the actual tree (`cargo test --release`: 847 passed / 0 failed, 81s). What blocks a clean SHIP is one mis-wired RED path, one whole countermeasure missing, the old blanket-cooldown surviving in the fleet path, and a deployment reality where the reviewed code is not what's running.

### Countermeasure verification matrix

| # | Catalog countermeasure | Exists | Wired (reader) | Runnable check | Status |
|---|---|---|---|---|---|
| 1 | Metric-freshness ledger, unobservable=RED halt | `improver/freshness.rs:347` (short_circuit), `:466` (unobservable_halt) | `iteration.rs:53` — top of `one_iteration`, before any git/gate/pi. Ledger read `:163`/written `:178` each decision | Probe live-verified: `kairos tools/fitness.py --freshness` → `{"metric_id":"settled_usd_15m","n_samples":13,"observable":true}` rc=0 against real tables in `.state/kairos.db` (trades/decisions/shadow_decisions) | PASS (see finding 2 for the halt's paging path) |
| 2 | Provider budget ledger, canaried fallbacks, exponential per-endpoint parking | `improver/budget.rs:143` (park 900→21600 exp), `:155` (headroom preflight), `:392` (choose_endpoint requires different endpoint + canary ≤24h), `:583` (canary = real nonce'd implement-task, diff-verified, identical run_pi argv) | `pi.rs:457-467` (preflight/parked refusal), `:517` (record_call), `:668-673` (429→park, success→reset) | budget.rs unit tests in the 847 | PASS for improver lanes; FAIL for fleet path (finding 5) |
| 3 | Closed action registry, TTL'd escalations, no operator dead-ends | `actions.json` + `actions.rs:29` (closed kind set), `:148` (total execute_action) | `supervisor.rs:973` (ttl_escalation_step in recover), fallback_runs bounded → daily deduped page | Closure contract test `actions.rs:331` iterates `supervisor::diagnose_categories()`; anti-drift source-scan test `supervisor.rs:2935` | PASS except finding 2 |
| 4 | Host-independent liveness (out-of-band supervisor) | `tools/install_sentinel.ps1`; watchdog restart budget `watchdog.rs:440`, standstill marker `:79` | **Verified live**: schtasks "Solomon Sentinel" exists, status Running, 5-min cadence; `runtime/_sentinel_heartbeat.json` fresh (17:40:41Z, checked 17:45Z); sweep PID 4912 running the new exe | schtasks query + heartbeat file | PASS |
| 5 | Progress ledger / quarantine (anti retry-theater) | `improver/progress.rs:47` (hash(job_kind\|goal\|diagnosis)), `:199` (N=3 strikes→24h quarantine, disk-persisted) | Selection: `iteration.rs:416`; terminals record_outcome at 15+ call sites (`iteration.rs:500-1078`); state-hash pre/post over base-tip/backlog/history/gates | progress.rs + iteration wiring unit tests in the 847 | PASS (minor: finding 8) |
| 6 | Controller-clean preflight + config provenance | `provenance.rs:243` (controller_clean), `:144` (tripwire w/ grace, HOLD_META TTL, .env adoption); `run.rs:256` hard refusal; `watchdog.rs:709` surface+page | **Verified firing live**: `_watchdog.out.log` 17:25-17:35Z shows "CONTROLLER DIRTY … refuses meta-work" during today's build; HOLD_META honored by `freshness.rs:306` | provenance tests in the 847 | PARTIAL (findings 1, 4, 6) |
| 7 | Task-size calibration table per model | **ABSENT** — no `ship_rate`/`size_class`/calibration anywhere in `src-tauri/src` | n/a | n/a | FAIL (finding 3) |
| 8 | Telemetry dedup | Page markers 24h/category `actions.rs:272-306`; standstill once-until-recovery; provenance transitions-only | wired | tested | PASS as far as verifiable — but the catalog itself is truncated mid-countermeasure-8 (finding 9) |

Workstream 0: yes — `7d1a278 operator: rsi-v3 baseline snapshot` landed, current tree is clean and on main. But see finding 4 for what came after it.

### Ranked findings

1. **HIGH — The running control plane is not the reviewed build.** GUI process PID 23100 = repo-root `Solomon.exe` built Jul 5 23:19 — pre-baseline, pre-v3, containing zero countermeasures. `tools/build_safe.ps1` correctly refused to overwrite a live exe, so root exe is stale; only the Sentinel runs the fresh `src-tauri/target/release/solomon.exe` (Jul 6 13:15). `control/runner.rs:52` spawns `current_exe()` — any lane started from the open GUI runs pre-v3 code with no freshness gate, no budget ledger, no quarantine. Fix: close GUI → `build_safe.ps1 -CopyOnly` → relaunch. Until then, v3 is deployed only on the watchdog path.

2. **HIGH — `metric_unobservable` is dead config; the flagship RED halt mis-routes.** `actions.json:24` maps `metric_unobservable → page_operator_deduped`, but `supervisor.rs` never emits that category: the freshness halt's heartbeat (`status=error, reason=metric_unobservable`, `freshness.rs:475-479`) falls through `diagnose()` to `unknown_error` (`supervisor.rs:512-513`) → actions.json `unknown_error → restart_lane` — three pointless lane restarts (a restart cannot fix a broken probe) before a generic daily page that buries the tier-1 identity. `freshness.rs:462-465`'s comment "WS3 maps it to a TTL'd page" is false. The halt itself works (bounded, zero token spend); the actuation routing reproduces catalog #3's shape in miniature. Fix: one `reason == Some("metric_unobservable")` branch in `diagnose()`.

3. **HIGH — Countermeasure #7 (task-size calibration) is not implemented at all.** No `{model, size_class, ship_rate}` table, no decompose-on-low-ship-rate, no idea-dedupe against the done-ledger in the v3 modules. `REQUIREMENTS-2026-07-06.md:13-14` states the catalog is the floor.

4. **MED-HIGH — Self-exempt control plane, 2026 edition.** `af2fe89` (the v3 build itself) is a direct-to-local-main commit — no PR (the Gen-1 keystone invariant), unpushed (origin/main is 2 behind), and its subject tag `rsi-v3:` violates PROVENANCE.md's own convention (`operator:`/`rsi:` only, "no third category") while adding watched file `actions.json`. PROVENANCE.md declares `git log --oneline -- repos.json ops.json actions.json` as "the check" — it fails on its first output line. No test or tripwire reads commit tags: the tag convention is a write-only ledger. Also `provenance.rs:257` checks branch name only, so a weeks-stale unpushed main is "clean".

5. **MED — The condemned 24h blanket cooldown survives in the fleet path.** `fleet.rs:346-347` `set_cooldown` on any quota error + `repos.json` autopilot `cooldown_s: 86400` — the exact "one quota error = fleet asleep a day" mechanism catalog #2 autopsied. Only improver lanes got per-endpoint exponential parking. The autopilot's 5 targets still live under the old regime.

6. **MED — The engine's own writes trigger false CONTROLLER DIRTY pages.** `proc.rs:316-322` tmp+rename (non-atomic replace window on Windows) on `repos.json` races the Sentinel's `git status`: observed live at 17:45:45Z — sweep saw `[ D repos.json]`, paged and stamped `_controller_dirty_paged` (17:45:17Z) for a file that was present and clean seconds later. Recovery clears the marker (`watchdog.rs:737`), so every future race re-pages — a periodic false-RED generator, catalog #8's alert-fatigue mechanism. Fix: confirm dirt across two consecutive probes before paging.

7. **MED — kairos, the stated point of v3, has never run a cycle.** `runtime/kairos/` holds one Jul 4 supervisor record; latest sweep says 0/9 lanes running. freshness/progress/budget ledgers exist only in tests. Also the kairos fallback (`glm-5.2` → `minimax-m3`, both ollama-cloud) is same-account quota monoculture — operator-imposed (requirement 1), but a single account-level 429 wave still parks both endpoints.

8. **LOW — "forcing ideate next cycle" is a no-op.** `progress.rs:300-317` all-quarantined bail deletes a `_ideate_done` marker nothing reads (the code admits it), and kairos has `pipeline.ideate=false` — a fully-quarantined backlog idles until 24h expiry instead of growing the menu. Bounded and token-free, but the documented degraded mode doesn't exist.

9. **LOW — The failure catalog itself is truncated.** `docs/rsi/FAILURE-CATALOG-2026-07-06.md` ends mid-sentence at countermeasure #8 ("every page class requires a **d", 10,104 bytes). The committed design input is incomplete and nobody flagged it; full #8 compliance is unverifiable against operator intent.

### Verified positives (evidence)
- `cargo test --release`: **847 passed / 0 failed** on the exact tree (matches the commit's claimed gate).
- Freshness probe executed live: real JSON contract, real settled rows (n=13), rc=0 — no phantom-table probe.
- Closure contract test (`actions.rs:331`) + diagnose-literal anti-drift test (`supervisor.rs:2935`) are real build-time assertions, not comments.
- Canary is a genuine diff-verified implement-task with nonce (`budget.rs:583-645`), sharing run_pi's exact argv surface — the asmodeus dead-fallback and nemotron narrated-success shapes are both blocked.
- Sentinel scheduled task live and firing every 5 min; controller-clean tripwire observed refusing meta-work during today's dirty-tree build window — the gate demonstrably applies to the controller itself.
- Janitor is bounded and ledger-safe: protected list matches actual ledger filenames (`_provider_budget.json`, `cycle_budget.json`, `progress.json`), single `.1` rotation generation, 200MB dir caps, honest compaction markers.

Key paths: `C:\Users\Cayleb\Desktop\workspace\solomon\src-tauri\src\{actions.rs, supervisor.rs, provenance.rs, janitor.rs, watchdog.rs, improver\{freshness.rs, budget.rs, progress.rs, tiers.rs, pi.rs, iteration.rs, run.rs}}`, `C:\Users\Cayleb\Desktop\workspace\solomon\actions.json`, `C:\Users\Cayleb\Desktop\workspace\solomon\docs\rsi\PROVENANCE.md`.
